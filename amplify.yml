version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Skipping AWS Amplify build phase."
        - echo "Using pre-compiled web artifacts from the build/web directory."
  artifacts:
    baseDirectory: build/web
    files:
      - '**/*'
